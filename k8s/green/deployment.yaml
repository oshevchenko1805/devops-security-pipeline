apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-green
  labels:
    app: devops-app
    version: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops-app
      version: green
  template:
    metadata:
      labels:
        app: devops-app
        version: green
    spec:
      containers:
      - name: app
        image: oshevchenko1805/devops-security-pipeline:green
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: "green"
        - name: APP_VERSION
          value: "2.0.0"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 3
